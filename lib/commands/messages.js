module.exports = {
  initGreeting: `This utility will walk you through creating a mvnpm.pom file.
It only covers the basic required items, in a format reusable for a 
Maven Project Object Model (pom.xml) file.

See Maven documentation on additional fields and exactly what they do.

Use 'mvnpm install <pkg> --save' afterwards to install a package and
save it as a dependency in the mvnpm.pom file.

Press ^C at any time to quit.
`,
  item: "",
  also: `
`
};
